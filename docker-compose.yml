services:
  postgres:
    image: postgres:16
    container_name: kandan-postgres
    environment:
      POSTGRES_DB: kandan
      POSTGRES_USER: kandan
      POSTGRES_PASSWORD: kandan
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

